To start off, clone the repo 
```bash
git clone https://github.com/Srindot/YOLO_Aerial_vision.git
```
To build the Conda Environment, run the following command :
```bash
conda env create -f environment.yml
```